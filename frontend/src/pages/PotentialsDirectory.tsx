/**
 * PotentialsDirectory — Public directory listing placeholder.
 * Full implementation (filters, pagination, grid) in Phase 13B.
 *
 * @see docs/product/FEATURE_SPEC.md PG-02 Directory Grid
 */
export default function PotentialsDirectory() {
  return (
    <div className="flex min-h-[60vh] flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-2xl font-bold text-[--neutral-900]">
        Direktori Potensi Desa
      </h1>
      <p className="max-w-sm text-[--neutral-500]">
        Direktori akan diimplementasikan pada fase berikutnya.
      </p>
    </div>
  );
}
